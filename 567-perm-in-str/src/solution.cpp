#include "../include/solution.hpp"

Solution::Solution() = default;
Solution::~Solution() = default;
bool Solution::checkInclusion(std::string s1, std::string s2) {
  bool rslt = false;

  size_t left, right, lastIndex, counter;
  lastIndex = s2.length()-1;
  left  = 0;
  right = s1.length()-1;
  counter = 0;

  while (right <= lastIndex) {

  }

  return rslt;
}